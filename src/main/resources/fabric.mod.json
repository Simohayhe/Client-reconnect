{
  "schemaVersion": 1,
  "id": "client-reconnect",
  "version": "${version}",
  "name": "ClientReconnect",
  "description": "サーバー切断時に自動で再接続するMOD",
  "authors": [],
  "contact": {},
  "license": "All-Rights-Reserved",
  "environment": "client",
  "entrypoints": {
    "main": [
      "net.simohaya.clientReconnect.ClientReconnect"
    ],
    "client": [
      "net.simohaya.clientReconnect.client.ClientReconnectClient"
    ]
  },
  "mixins": [
    "client-reconnect.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric-api": "*",
    "minecraft": "${minecraft_version}"
  }
}